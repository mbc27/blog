# 📁 文件上传路径配置说明

## 🔧 问题解决

### 问题描述
在本地开发时使用绝对路径 `d:/Trae-AI/Project/blog/uploads/`，但部署到服务器时该路径不存在，导致文件上传失败。

### 解决方案
使用Spring Boot的多环境配置，根据不同环境自动选择合适的文件上传路径。

## 📋 配置文件说明

### 1. 主配置文件 `application.yml`
```yaml
# 默认使用开发环境配置
spring:
  profiles:
    active: dev  # 默认激活开发环境
```

### 2. 开发环境配置 `application-dev.yml`
```yaml
# 开发环境 - 使用绝对路径
file:
  upload:
    path: d:/Trae-AI/Project/blog/uploads/  # 本地开发路径
    url: http://localhost:8080/uploads/
```

### 3. 生产环境配置 `application-prod.yml`
```yaml
# 生产环境 - 使用相对路径
file:
  upload:
    path: ./uploads/  # 相对于jar包运行目录
    url: http://your-domain.com/uploads/  # 修改为实际域名
```

## 🚀 部署方式

### 方式一：修改配置文件（推荐）
1. 修改 `application-prod.yml` 中的配置：
```yaml
file:
  upload:
    path: /www/wwwroot/blog/uploads/  # 服务器实际路径
    url: http://your-domain.com/uploads/
```

2. 启动时指定生产环境：
```bash
java -jar blog-api.jar --spring.profiles.active=prod
```

### 方式二：启动时指定参数
```bash
java -jar blog-api.jar \
  --spring.profiles.active=prod \
  --file.upload.path=/www/wwwroot/blog/uploads/ \
  --file.upload.url=http://your-domain.com/uploads/
```

### 方式三：环境变量
```bash
export SPRING_PROFILES_ACTIVE=prod
export FILE_UPLOAD_PATH=/www/wwwroot/blog/uploads/
export FILE_UPLOAD_URL=http://your-domain.com/uploads/
java -jar blog-api.jar
```

## 📂 目录结构建议

### 开发环境
```
d:/Trae-AI/Project/blog/
├── backend/
├── frontend/
└── uploads/          # 文件上传目录
    └── 2025/
        └── 09/
            └── 11/
```

### 生产环境（宝塔面板）
```
/www/wwwroot/blog/
├── backend/
├── frontend/
└── uploads/          # 文件上传目录
    └── 2025/
        └── 09/
            └── 11/
```

## 🔍 验证配置

### 1. 检查当前配置
启动应用后查看日志：
```
Upload path config: ./uploads/
Normalized path: ./uploads/
Absolute path: /www/wwwroot/blog/uploads/
Static resource mapping: /uploads/** -> file:/www/wwwroot/blog/uploads/
Upload directory exists: true
```

### 2. 测试文件上传
```bash
curl -X POST \
  http://localhost:8080/api/upload \
  -H 'Content-Type: multipart/form-data' \
  -F 'file=@test.jpg'
```

### 3. 检查文件访问
```bash
curl http://localhost:8080/uploads/2025/09/11/test.jpg
```

## ⚠️ 注意事项

1. **权限设置**：确保应用有权限读写上传目录
```bash
chmod 755 /www/wwwroot/blog/uploads
chown -R www:www /www/wwwroot/blog/uploads
```

2. **Nginx配置**：确保Nginx正确代理文件访问
```nginx
location /uploads/ {
    alias /www/wwwroot/blog/uploads/;
    expires 30d;
}
```

3. **磁盘空间**：定期清理过期文件，避免磁盘空间不足

4. **备份策略**：定期备份上传的文件

## 🛠️ 常见问题

### Q1: 文件上传后无法访问
- 检查文件路径是否正确
- 检查文件权限设置
- 检查Nginx配置

### Q2: 目录创建失败
- 检查父目录权限
- 检查磁盘空间
- 检查路径格式

### Q3: 跨平台路径问题
- Windows使用 `\` 或 `/`
- Linux使用 `/`
- 建议统一使用 `/`

## 📝 配置模板

### 宝塔面板部署配置
```yaml
# application-prod.yml
server:
  port: 8080

spring:
  profiles:
    active: prod
  datasource:
    url: jdbc:mysql://localhost:3306/blog
    username: blog_user
    password: your_password

file:
  upload:
    path: /www/wwwroot/blog/uploads/
    url: https://your-domain.com/uploads/

jwt:
  secret: your-production-secret-key
```

### Docker部署配置
```yaml
file:
  upload:
    path: /app/uploads/
    url: http://localhost:8080/uploads/
```

现在您可以根据部署环境选择合适的配置方式！